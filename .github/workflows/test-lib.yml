name: Hellow world workflow

on:
  push:
    - "**"

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print Hello world
      run: echo "Hello world & JP"